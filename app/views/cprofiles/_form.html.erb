<%= form_for(cprofile) do |f| %>
  <% if cprofile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cprofile.errors.count, "error") %> prohibited this cprofile from being saved:</h2>

      <ul>
      <% cprofile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :skills %>
    <%= f.text_area :skills %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
