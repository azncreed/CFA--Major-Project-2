<%= form_for(company_profile) do |f| %>
  <% if company_profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company_profile.errors.count, "error") %> prohibited this company_profile from being saved:</h2>

      <ul>
      <% company_profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :skills %>
    <%= f.text_field :skills %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
